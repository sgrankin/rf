# mvstmt: move statements including if to helper function
mv x.go:/y :=/,/^	}/ checkHelper
-- x.go --
package p

func f(x int) {
	y := x + 1
	if y > 0 {
		println(y)
	}
}
-- stdout --
diff old/x.go new/x.go
--- old/x.go
+++ new/x.go
@@ -1,6 +1,11 @@
 package p

 func f(x int) {
+	checkHelper(x)
+
+}
+
+func checkHelper(x int) {
 	y := x + 1
 	if y > 0 {
 		println(y)
