# ex: import after var/type is too late
ex {
	var x int
	import "fmt"
	x + 1 -> x + 2
}
-- x.go --
package m

import "fmt"

func F() { fmt.Println(1 + 1) }
-- stderr --
usage: parsing import "fmt": import too late
