# mvstmt: test isTerm switch with various termination patterns
mv F:/BEGIN/+0,/END/-0 New
-- x.go --
package m

func F(x int) int {
	// BEGIN
	switch x {
	case 0:
		if x >= 0 {
			return 0
		} else {
			return -1
		}
	case 1:
		for {
			return 1
		}
	default:
		return x
	}
	// END
}
-- stdout --
diff old/x.go new/x.go
--- old/x.go
+++ new/x.go
@@ -2,6 +2,11 @@

 func F(x int) int {
 	// BEGIN
+	return New(x)
+	// END
+}
+
+func New(x int) int {
 	switch x {
 	case 0:
 		if x >= 0 {
@@ -16,5 +21,4 @@
 	default:
 		return x
 	}
-	// END
 }
