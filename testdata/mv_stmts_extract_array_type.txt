# mvstmt: extract code that returns an array value (printType array coverage)
mv x.go:4 NewFunc
-- x.go --
package m

func F() {
	a := [3]int{1, 2, 3}
	println(a[0])
}
-- stdout --
diff old/x.go new/x.go
--- old/x.go
+++ new/x.go
@@ -1,6 +1,11 @@
 package m

 func F() {
-	a := [3]int{1, 2, 3}
+	a := NewFunc()
 	println(a[0])
 }
+
+func NewFunc() [3]int {
+	a := [3]int{1, 2, 3}
+	return a
+}
