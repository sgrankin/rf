# mvstmt: extract code that returns a chan value (printType chan coverage)
mv x.go:4 NewFunc
-- x.go --
package m

func F() {
	ch := make(chan int)
	close(ch)
}
-- stdout --
diff old/x.go new/x.go
--- old/x.go
+++ new/x.go
@@ -1,6 +1,11 @@
 package m

 func F() {
-	ch := make(chan int)
+	ch := NewFunc()
 	close(ch)
 }
+
+func NewFunc() chan int {
+	ch := make(chan int)
+	return ch
+}
