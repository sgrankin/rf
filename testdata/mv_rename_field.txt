# mv: rename struct field (rewriteDefn Field outer path)
mv T.X T.Y
-- x.go --
package m

type T struct {
	X int
}

func F(t T) int {
	return t.X
}
-- stdout --
diff old/x.go new/x.go
--- old/x.go
+++ new/x.go
@@ -1,9 +1,9 @@
 package m

 type T struct {
-	X int
+	Y int
 }

 func F(t T) int {
-	return t.X
+	return t.Y
 }
