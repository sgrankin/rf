mv x.go:4,10 helper
-- x.go --
package p

func f(ch chan int) int {
	x := 0
	select {
	case v := <-ch:
		return v
	default:
		return x
	}
	return -1
}
-- stdout --
diff old/x.go new/x.go
--- old/x.go
+++ new/x.go
@@ -1,6 +1,11 @@
 package p

 func f(ch chan int) int {
+	return helper(ch)
+	return -1
+}
+
+func helper(ch chan int) int {
 	x := 0
 	select {
 	case v := <-ch:
@@ -8,5 +13,4 @@
 	default:
 		return x
 	}
-	return -1
 }
