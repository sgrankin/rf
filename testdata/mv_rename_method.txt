# mv: rename method within same type (old.Outer.Obj == newOuter.Obj path)
mv T.M T.N
-- x.go --
package m

type T struct{}

func (t T) M() int { return 1 }

func F() int {
	var t T
	return t.M()
}
-- stdout --
diff old/x.go new/x.go
--- old/x.go
+++ new/x.go
@@ -2,9 +2,9 @@

 type T struct{}

-func (t T) M() int { return 1 }
+func (t T) N() int { return 1 }

 func F() int {
 	var t T
-	return t.M()
+	return t.N()
 }
