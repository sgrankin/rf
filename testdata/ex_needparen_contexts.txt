# ex: needParen with binary expr in selector, call, index, slice contexts
ex {
	import "bytes"
	var b *bytes.Buffer
	b -> b
}
-- x.go --
package m

import "bytes"

func F() {
	var b *bytes.Buffer
	b.Len()
	_ = b
}
