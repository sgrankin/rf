ex {
	var ch chan int
	ch -> make(chan int)
}
-- x.go --
package m

func F(ch chan int) {
	_ = ch
}
-- stdout --
diff old/x.go new/x.go
--- old/x.go
+++ new/x.go
@@ -1,5 +1,5 @@
 package m

 func F(ch chan int) {
-	_ = ch
+	_ = make(chan int)
 }
